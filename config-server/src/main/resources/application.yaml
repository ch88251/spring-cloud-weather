server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ch88251/spring-cloud-weather
          search-paths: config-repo
  application:
    name: Config Server
